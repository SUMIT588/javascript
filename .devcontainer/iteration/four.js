const myObject = {
    js : "javascript", 
    rb : "ruby",
    cpp : "c++"
}

for (const key in myObject) {
        // console.log(`${key} is ${myObject[key]}`)
    }

    const arr = ["js", "cpp", "swift", "rb"]

    for (const key in arr ) {
       console.log(key, " is", arr[key])
        }
    